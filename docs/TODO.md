# TODO — что делаем дальше

Живая очередь. Крупные вехи и их критерии приёмки — в [COURSE](COURSE.md), тут только ближайшие шаги.

## Сейчас

- [ ] ▶️ **НАЧАТЬ ОТСЮДА: перенести `isRouteLoading` в Pinia.** Остановились здесь 25.09.2026.
  Сейчас флаг живёт модульным `ref` в `src/router/index.ts` и оттуда импортируется в `App.vue`.
  Сделать: стор `src/stores/ui.ts` с id `ui`, флаг + действия включить/выключить; роутер дёргает
  стор в `beforeEach`/`afterEach`/`onError`; `App.vue` читает из стора; `export const
  isRouteLoading` из роутера убрать. Проверка: `/about` с throttling — полоска видна;
  `npm run type-check` молчит; в DevTools → Store появился `ui` и флаг мигает на переходах.
  Образец стора — `src/stores/counter.ts`, он уже разобран.
  ⚠️ Две известные ловушки: (1) **где именно** вызывать `useUiStore()` в файле роутера,
  (2) **как** доставать флаг из стора в `App.vue`. Обе выглядят как «сделал правильно, но не
  работает». Сначала читать текст ошибки в консоли — там половина ответа.
- [ ] **Что стор отдаёт наружу** — инварианты, `readonly` vs прямой `ref`, `$patch`/`$reset`.
  Отложено 25.09.2026, разбирать на визарде со сложным состоянием, не на счётчике.
  Материал: `KNOWLEDGE/notes/Vue/Что стор отдаёт наружу — инварианты и чёрный ход.md`.
- [ ] **Первая Pinia — корзина в `shop`.** Стор с товарами и добавлением в корзину; счётчик
  корзины показать в `App.vue` рядом с пунктом меню «Shop». Смысл: состояние меняет один
  компонент, показывает другой, общего родителя нет — стор становится единственным нормальным
  решением, а не упражнением. Перед этим прочитать `src/stores/counter.ts` из скаффолда.
  Дальше то же самое повторяется в пицца-визарде на сумме заказа.
- [ ] **Реактивность вплотную: `computed` и `watch`.** Запланировано на 25.09.2026 — механика
  подзабыта, нужен не словарь, а разбор: как Vue узнаёт, от чего зависит `computed`; почему он
  ленивый и кэшируется; `watch` vs `watchEffect`; опции `immediate` и `deep`; отмена
  предыдущего запроса при новом вводе (пригодится в поле адреса с подсказками).
- [ ] **Веха −1, заходы 4 и 5 — доделать словарь.** Отложено с 23.09.2026.
  Осталось: маршрут и его параметры · вложенные маршруты (`children`) · гард (`beforeEnter`) · `onMounted`.
  Пройдено: `ref` · `computed` · `watch` · пропсы и события · слоты · `provide/inject` · композабл · стор —
  см. [vue-словарь](../../KNOWLEDGE/cheatsheets/vue-словарь.md).
- [ ] **Разобрать скаффолд `create-vue`.** Пройтись по файлам, которые приехали с установкой, и
  понять, что каждый делает: `main.ts`, `router/index.ts`, `stores/counter.ts`, три `tsconfig.*.json`,
  `vite.config.ts` vs `vitest.config.ts`, `env.d.ts`, `App.vue`. Формат — вопрос/ответ, находки
  складывать в `KNOWLEDGE/notes/`.

## Дальше по курсу

- [ ] Веха 0 — todo-лист (типы, `v-for`, computed-фильтр, Pinia вместо локального `ref`, `localStorage`, фильтр в URL)
- [ ] Веха 0.5 — первый e2e своими руками
- [ ] Веха 1 — шаги визарда через роутер (`?step=`)
- [ ] Веха 2 — стор
- [ ] Веха 3 — формы и валидация
- [ ] Веха 4 — асинхронность и ошибки
- [ ] Веха 5 — восстановление состояния
- [ ] Веха 5.5 — эмуляция авторизации
- [ ] Веха 6 — Laravel под тем же контрактом

## Хвосты и мелочи

- [ ] Почистить скаффолд: `HelloWorld.vue`, `TheWelcome.vue`, `WelcomeItem.vue`, иконки,
  `stores/counter.ts`, `views/AboutView.vue` — удалить, когда начнём веху 0.
  Сначала прочитать, потом удалять: это учебный материал, а не мусор.
- [ ] **Увидеть 404 на прямом заходе живьём** (отложено 24.09.2026, принято на веру).
  `npm run build` → `python3 -m http.server -d dist 8000` → зайти на `/about` и нажать F5 → 404.
  Потом `npm run preview` — та же сборка, но с SPA-fallback, F5 работает.
  Суть: клик внутри приложения обрабатывает роутер в браузере, прямой заход — сервер;
  в бою серверу нужен `try_files $uri $uri/ /index.html`. Актуально станет на деплое.
